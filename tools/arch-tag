#!/bin/bash -e

uname="$(uname -m)"

if [[ "$uname" =~ "arm" ]]; then
  echo "armhf"
elif [[ "$uname" == "x86_64" ]]; then
  echo "amd64"
elif [[ "$uanme" == "ppc64le" ]]; then
  # the power8 architecture is ppc64 little endian and corresponds to debian / ubuntu arch designation "ppc64el"
  echo "ppc64el"
else
  (>&2 echo "Unknown architecture $uname")
  exit 1
fi
