Content in this directory is source data for dynamically-adjusted filesystem
content *specific to wiotp installations*. Each file may be handled differently
and its filesystem destination is not knowable from the directory structure in
this build directory.

Consult the `Makefile` for more information


